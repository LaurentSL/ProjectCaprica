using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColonyView : MonoBehaviour {

    public object Colony;

    // Gets called every time we become active (including possibly on scene load)
    private void OnEnable ()
    {
        // Update our datat elements based on the selected colony
    }

    // Use this for one-time initialization, AFTER OnEnable is called
    void Start () {
		
	}
	
	void Update () {
		
	}

}
